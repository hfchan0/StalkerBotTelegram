[project]
name = "instagram-archive-monitor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["instaloader>=4.14,<5", "requests>=2.32,<3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
